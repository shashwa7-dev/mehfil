// X/Twitter reads its own tag rather than falling back to og:image, so the
// card needs its own route. Same artwork — re-exported rather than duplicated.
export { default, size, contentType } from "./opengraph-image";

export const alt = "Mehfil — browse and play golden-era Hindi film music";
